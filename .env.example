# ===========================================
# CONFIGURACIÓN DEL SERVIDOR
# ===========================================
NODE_ENV=production
PORT=3001

# ===========================================
# BASE DE DATOS MYSQL
# ===========================================
DB_HOST=localhost
DB_USER=tu_usuario_mysql
DB_PASSWORD=tu_contraseña_segura
DB_DATABASE=sistema_fletes
DB_PORT=3306

# ===========================================
# JWT - TOKENS DE AUTENTICACIÓN
# ===========================================
# IMPORTANTE: Genera claves seguras de al menos 32 caracteres
# Puedes usar: openssl rand -base64 32
JWT_SECRET=tu_clave_secreta_jwt_minimo_32_caracteres
JWT_REFRESH_SECRET=tu_clave_secreta_refresh_minimo_32_caracteres

# ===========================================
# CORS - ORÍGENES PERMITIDOS (opcional)
# ===========================================
# ALLOWED_ORIGINS=https://tudominio.com,https://www.tudominio.com
